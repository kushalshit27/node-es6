# node-es6
node server app written in ES6

### Installation
node-es6 commands.
Install:
```sh
$ cd node-es6
$ npm install 
```
Build and start the server.

```sh
$ npm run build
$ npm start
$ npm run clean
```
